{
  "name": "hotel-management",
  "version": "1.0.0",
  "description": "Hotel management system built for FNF",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nahid Iftekhar",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
